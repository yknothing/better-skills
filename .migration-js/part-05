fs.rmSync(abs(".migration-components"), { recursive: true, force: true });

console.log(`Migrated ${selfSkills.length} self-developed and ${externalSkills.length} external identities to ${PREFIX}*.`);
